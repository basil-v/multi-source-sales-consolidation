# Copy to .env and fill in to target SQL Server instead of local SQLite.
# Leave unset to use the local SQLite output (db/output.db).
DB_SERVER=your-server.database.windows.net
DB_NAME=your_database
DB_USERNAME=your_user
DB_PASSWORD=your_password
